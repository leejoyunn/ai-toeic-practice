import Link from"next/link";import{redirect}from"next/navigation";import{AppShell}from"@/components/layout/app-shell";import{getCurrentUser}from"@/lib/supabase/auth";import{createSupabaseServerClient}from"@/lib/supabase/server";import{loadMockTest}from"@/lib/toeic/mock-test/load";import{MockResult}from"@/features/mock-test/mock-result";
export default async function MockResultPage({params}:{params:Promise<{id:string}>}){const{id}=await params,user=await getCurrentUser();if(!user)redirect("/login");const supabase=await createSupabaseServerClient();if(!supabase)throw new Error("尚未設定 Supabase。");const data=await loadMockTest(supabase,user.id,id,true);if(data.test.status!=="completed")redirect(`/mock-test/${id}`);return <AppShell user={user} active="practice"><main className="page-content"><Link href="/mock-test">← Mock Test History</Link><MockResult data={data}/></main></AppShell>}
