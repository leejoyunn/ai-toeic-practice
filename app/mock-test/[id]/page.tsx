import{redirect}from"next/navigation";import{AppShell}from"@/components/layout/app-shell";import{getCurrentUser}from"@/lib/supabase/auth";import{createSupabaseServerClient}from"@/lib/supabase/server";import{loadMockTest}from"@/lib/toeic/mock-test/load";import{MockTestFlow}from"@/features/mock-test/mock-test-flow";
export default async function MockTestRunPage({params}:{params:Promise<{id:string}>}){const{id}=await params,user=await getCurrentUser();if(!user)redirect("/login");const supabase=await createSupabaseServerClient();if(!supabase)throw new Error("尚未設定 Supabase。");const data=await loadMockTest(supabase,user.id,id,false);if(data.test.status==="completed")redirect(`/mock-test/${id}/result`);return <AppShell user={user} active="practice"><MockTestFlow initial={data}/></AppShell>}
