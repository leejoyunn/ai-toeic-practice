import{NextResponse}from"next/server";import{z}from"zod";import{createSupabaseServerClient}from"@/lib/supabase/server";
export async function PATCH(request:Request,{params}:{params:Promise<{questionId:string}>}){try{const{questionId}=await params;z.string().uuid().parse(questionId);const{resolved}=z.object({resolved:z.boolean()}).parse(await request.json());const supabase=await createSupabaseServerClient();if(!supabase)return NextResponse.json({error:"尚未設定 Supabase。"},{status:503});const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"請先登入。"},{status:401});const{error}=await supabase.from("wrong_answers").update({resolved}).eq("user_id",user.id).eq("question_id",questionId);if(error)throw new Error(error.message);return NextResponse.json({resolved});}catch(error){return NextResponse.json({error:error instanceof Error?`無法更新錯題狀態：${error.message}`:"無法更新錯題狀態。"},{status:400});}}
