import { AppShell } from "@/components/layout/app-shell";import { getCurrentUser } from "@/lib/supabase/auth";import Link from "next/link";
export default async function Page({params}:{params:Promise<{mode:string}>}){const [{mode},user]=await Promise.all([params,getCurrentUser()]);const names:Record<string,string>={listening:"聽力練習",reading:"閱讀練習",recommended:"今日推薦"};return <AppShell user={user} active="practice"><main className="placeholder-page"><section className="placeholder-card"><p className="eyebrow">COMING NEXT</p><h1>{names[mode]??"練習"}</h1><p>此入口已保留。題目生成與作答流程會依分階段規劃在後續實作，不會用固定題庫假裝完成。</p><Link className="button" href="/">返回首頁</Link></section></main></AppShell>}
