"use client";import{useState}from"react";const choices=[500,550,600,650,700,750,800,850,900];
export function TargetScoreForm({initialScore}:{initialScore:number}){const[score,setScore]=useState(initialScore),[message,setMessage]=useState("");async function save(){setMessage("儲存中…");const response=await fetch("/api/profile/target-score",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({targetScore:score})});const payload=await response.json()as{error?:string};setMessage(response.ok?"目標分數已儲存。":payload.error??"儲存失敗。")}return <div className="target-score-form"><label htmlFor="target-score">目標 TOEIC 分數</label><input id="target-score" type="number" min={300} max={990} step={5} list="target-score-options" value={score} onChange={(event)=>setScore(Number(event.target.value))}/><datalist id="target-score-options">{choices.map((value)=><option value={value} key={value}/>)}</datalist><button className="button button-small" onClick={save}>儲存目標</button>{message&&<small>{message}</small>}</div>}
