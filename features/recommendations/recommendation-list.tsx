"use client";
import Link from"next/link";import{useState}from"react";import{BookOpen,ChevronRight,Headphones}from"lucide-react";import type{Recommendation,PracticeMode}from"@/lib/toeic/recommendation/plan";
export function RecommendationList({all,reading,listening}:{all:Recommendation[];reading:Recommendation[];listening:Recommendation[]}){const[mode,setMode]=useState<PracticeMode>("all"),items=mode==="reading"?reading:mode==="listening"?listening:all;return <section className="section-block"><div className="section-heading"><div><p className="eyebrow">RECOMMENDED PRACTICE</p><h2>今日建議</h2></div><div className="segmented compact"><button className={mode==="all"?"selected":""} onClick={()=>setMode("all")}>全部</button><button className={mode==="reading"?"selected":""} onClick={()=>setMode("reading")}>只做 Reading</button><button className={mode==="listening"?"selected":""} onClick={()=>setMode("listening")}>只做 Listening</button></div></div><div className="task-list">{items.map((item)=><Link href={item.href} className="task-card" key={`${item.kind}-${item.part??0}`}><span className={`task-icon ${item.part&&item.part<=4?"blue":"mint"}`}>{item.part&&item.part<=4?<Headphones/>:<BookOpen/>}</span><span><strong>{item.title}</strong><small>{item.detail}</small></span><ChevronRight/></Link>)}</div></section>}
