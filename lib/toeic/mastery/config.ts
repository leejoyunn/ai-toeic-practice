import type{Difficulty}from"@/types/toeic";
export const MASTERY_RECENT_WINDOW=10;
export type MasteryLevel="learning"|"improving"|"familiar"|"mastered";
export interface MasterySnapshot{attempts:number;correctCount:number;recentResults:boolean[];recentAccuracy:number;consecutiveCorrect:number;consecutiveWrong:number;masteryLevel:MasteryLevel}
export function calculateMastery(input:Omit<MasterySnapshot,"recentAccuracy"|"masteryLevel">):MasterySnapshot{const recentResults=input.recentResults.slice(-MASTERY_RECENT_WINDOW),recentAccuracy=recentResults.length?recentResults.filter(Boolean).length/recentResults.length*100:0;let masteryLevel:MasteryLevel="learning";if(input.attempts>=10&&recentAccuracy>=85&&input.consecutiveCorrect>=4&&input.consecutiveWrong===0)masteryLevel="mastered";else if(input.attempts>=6&&recentAccuracy>=70&&input.consecutiveCorrect>=2&&input.consecutiveWrong<2)masteryLevel="familiar";else if(input.attempts>=3&&recentAccuracy>=45&&input.consecutiveWrong<3)masteryLevel="improving";return{...input,recentResults,recentAccuracy,masteryLevel};}
export function masteryDifficulty(input:{currentEstimatedLevel:number;targetScore:number;masteryLevel:MasteryLevel;recentAccuracy:number;consecutiveCorrect:number;consecutiveWrong:number;requested?:Difficulty}){let difficulty:Difficulty=input.currentEstimatedLevel<470?"easy":"medium";if(input.masteryLevel==="learning"||input.recentAccuracy<45||input.consecutiveWrong>=2)difficulty="easy";else if(input.masteryLevel==="mastered"&&input.recentAccuracy>=85&&input.consecutiveCorrect>=4)difficulty=input.targetScore>=600?"hard":"medium";else if(input.masteryLevel==="familiar")difficulty="medium";if(input.requested&&!(input.consecutiveWrong>=2&&input.requested!=="easy"))difficulty=input.requested;return difficulty;}
export const masteryLabel:Record<MasteryLevel,string>={learning:"學習中",improving:"進步中",familiar:"熟悉",mastered:"已掌握"};
